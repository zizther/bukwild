.action-bar {
    @apply flex justify-between mt-8 px-2 py-2;

    &._border {
        @apply border-b border-t border-gray-200;
    }

    button {
        @apply text-gray-500 hover:text-gray-700;
    }
}
