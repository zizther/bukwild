.image-gallery {
    .row {
        @apply flex flex-wrap -mx-2;
    }
    .column {
        @apply flex-[100%] max-w-full px-2 md:flex-[50%] md:max-w-1/2 lg:flex-[33.33333%] lg:max-w-1/3;

        img {
            @apply align-middle mt-4 w-full rounded-lg md:rounded-xl;
        }
    }
}
